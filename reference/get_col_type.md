# Get Column Type from data.table Column

Returns a normalized type string for a data.table column.

## Usage

``` r
get_col_type(col)
```

## Arguments

- col:

  Column from a data.table.

## Value

Character string: "numeric", "integer", "factor", "character",
"logical", or the raw typeof.
