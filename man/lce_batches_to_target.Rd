% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lce_batches_to_target.R
\name{lce_batches_to_target}
\alias{lce_batches_to_target}
\title{Batches-to-Target Forecast}
\usage{
lce_batches_to_target(
  learner,
  batch_grid,
  target,
  crossing = "expected",
  probs = c(0.1, 0.5, 0.9)
)
}
\arguments{
\item{learner}{(\link{LearnerLCE})\cr
A trained LCE learner.}

\item{batch_grid}{(\code{integer()})\cr
Strictly increasing future \code{batch_nr} values to evaluate the crossing over.}

\item{target}{(\code{numeric(1)})\cr
Target performance value.}

\item{crossing}{(\code{character(1)})\cr
\code{"expected"} or \code{"observed"}.}

\item{probs}{(\code{numeric()})\cr
Probabilities at which to report crossing-batch quantiles.}
}
\value{
A \code{list} with:
\itemize{
\item \code{quantiles} (named \code{numeric()}): for each \code{probs}, the smallest grid batch at
which the crossing CDF reaches that probability, or \code{NA} if the grid never
does (the target is not reached by that quantile within the grid).
\item \code{p_never} (\code{numeric(1)}): probability the target is never reached within the
grid (\verb{1 -} the maximum crossing CDF).
\item \code{grid} (\link[data.table:data.table]{data.table::data.table}): columns \code{batch} and \code{cdf}, the
crossing-batch CDF over the grid.
}
}
\description{
Turns a trained \link{LearnerLCE} into a forecast of \emph{how many further batches} are
needed to reach a target performance \code{target}, by reading off the learner's
predictive over a future \code{batch_nr} grid. This is a reported transform, not a
predict type or measure: it summarises the per-batch predictive into a
crossing-batch distribution.

Two crossing semantics are supported (see the celecx research notes):
\itemize{
\item \code{crossing = "expected"} (default): the de-noised expected curve \code{f(b)}
crosses \code{target}. For the principled, monotone forecasters this law is fixed
by the per-batch marginals alone, so it is computed in closed form from the
\code{se_epistemic} (epistemic) standard error: the crossing CDF at grid batch
\code{b} is the epistemic probability that \code{f(b)} has passed \code{target}.
\item \code{crossing = "observed"}: the \emph{noisy realised} \code{y_b} crosses \code{target} -- the
literal stop time of an observe-and-stop run. This is a first-passage of a
correlated noisy sequence and needs the joint predictive, so it is estimated
from the learner's \code{samples} predict type (each column is a joint sample
path). The learner must support \code{"samples"} (the sample-based learners
\link{LearnerLCEBootstrap} and \link{LearnerLCESimulate} do). It is systematically
optimistic relative to the expected crossing.
}

The optimization direction (whether the target is reached from above or below)
and the predictive \link{lce_link} scale are read from the learner's training task,
which must therefore carry a \code{measure}.
}
