% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task_lce_best_so_far.R
\name{task_lce_best_so_far}
\alias{task_lce_best_so_far}
\title{Best-So-Far Optimization Trace as an LCE Task}
\usage{
task_lce_best_so_far(
  archive,
  link = "identity",
  id = "best_so_far",
  label = NA_character_
)
}
\arguments{
\item{archive}{(\link[bbotk:Archive]{bbotk::Archive})\cr
Archive of a completed single-target optimization run. Its codomain target
must be tagged \code{"minimize"} or \code{"maximize"} (a \code{"learn"}-tagged target has
no best value).}

\item{link}{(\code{character(1)})\cr
Name of the predictive \link{lce_link} for the resulting task. \code{"identity"} by
default; note that objective values are in general not sign-constrained,
so non-identity links only make sense for suitably bounded objectives.}

\item{id}{(\code{character(1)})\cr
Task id. Defaults to \code{"best_so_far"}.}

\item{label}{(\code{character(1)})\cr
Optional task label.}
}
\value{
\link{TaskLCE}.
}
\description{
Builds a \link{TaskLCE} whose target is the best objective value observed up to
(and including) each archive batch -- the progress curve of an optimization
run. This is the optimization-mode counterpart of the surrogate-performance
tasks built by \link{CallbackSurrogatePerformance} and
\code{\link[=replay_surrogate_performance]{replay_surrogate_performance()}}: the same LCE learners, measures, and
resamplings apply, forecasting future best-so-far values instead of future
model quality.

The task has one row per archive evaluation, carrying the archive's feature
and target columns in the \code{archive_x} / \code{archive_y} roles. The target column
\code{best_so_far} is constant within a batch (batches are evaluated as a whole,
so mid-batch improvements only become visible at the batch's end). The
optimization direction is taken from the archive codomain's single
minimize/maximize-tagged target and travels with the task via its stored
codomain; direction-dependent operations (e.g. the \code{"target_reached"}
predict type, \code{\link[=lce_batches_to_target]{lce_batches_to_target()}}) work without a task measure.
}
