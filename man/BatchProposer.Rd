% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BatchProposer.R
\name{BatchProposer}
\alias{BatchProposer}
\title{Batch Proposer}
\arguments{
\item{optimizer}{(\link[bbotk:OptimizerBatch]{bbotk::OptimizerBatch})\cr
Optimizer used for acquisition optimization.}

\item{terminator}{(\link[bbotk:Terminator]{bbotk::Terminator})\cr
Terminator used for acquisition optimization.}

\item{acq_function}{(\code{NULL} | \link[mlr3mbo:AcqFunction]{mlr3mbo::AcqFunction})\cr
Acquisition function. Can be set later via \verb{$acq_function}.}

\item{batch_strategy}{(\code{function})\cr
Batch selection strategy. Must have signature
\verb{function(candidates, scores, batch_size, surrogate, archive, search_space)}
and return integer indices into \code{candidates}.}

\item{pool_factor}{(\code{integer(1)})\cr
Pool size multiplier. Pool size is \code{n_candidates * pool_factor}.
Default is 10.}

\item{callbacks}{(\code{NULL} | \code{list()})\cr
Callbacks. Passed to parent \link[mlr3mbo:AcqOptimizer]{mlr3mbo::AcqOptimizer}.}

\item{...}{(ignored).}
}
\value{
\code{\link[data.table:data.table]{data.table::data.table()}} with 1 row per candidate.
}
\description{
Extension of \link[mlr3mbo:AcqOptimizer]{mlr3mbo::AcqOptimizer} that applies a batch selection strategy
to diversify the returned candidate set.

Creates a new BatchProposer.

Optimize the acquisition function and apply a batch selection strategy.

Print method.
}
\details{
This class implements a wrapper around the parent \link[mlr3mbo:AcqOptimizer]{mlr3mbo::AcqOptimizer}:
it temporarily requests a larger number of candidates, then down-selects to
the desired batch size using \code{batch_strategy}.

Batch strategies operate on a pool that is already ranked by the acquisition
optimizer and follow the convention: \strong{lower is better} (the sign is
adjusted automatically based on the acquisition codomain).
}
\section{Fields}{

\describe{
\item{\code{pool_factor}}{(\code{integer(1)})\cr
Multiplier for pool size. The internal pool size is
\code{n_candidates * pool_factor}.}

\item{\code{batch_strategy}}{(\code{function})\cr
Batch selection strategy.}
}}

\section{Parameters}{

Inherits all parameters from \link[mlr3mbo:AcqOptimizer]{mlr3mbo::AcqOptimizer}, in particular:
\itemize{
\item \code{n_candidates}: number of points to propose (batch size)
\item \code{warmstart}, \code{warmstart_size}, \code{skip_already_evaluated}, \code{catch_errors},
\code{logging_level}
}
}

