% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCEBootstrap.R
\name{mlr_learners_lce.bootstrap}
\alias{mlr_learners_lce.bootstrap}
\alias{LearnerLCEBootstrap}
\title{Residual Bootstrap LCE Learner Wrapper}
\arguments{
\item{learner}{(\link{LearnerLCE})\cr
Base LCE learner to wrap. Its predict type is forced to \code{"response"}
while the bootstrap replicates are fitted.}
}
\description{
Wraps an arbitrary base \link{LearnerLCE} and equips it with an SE prediction
via residual bootstrap on the per-batch fit.

Training proceeds as:
\enumerate{
\item Fit the base learner once on the training task; collect the per-batch
fitted values and the residuals on the \link{lce_link} scale
(\code{residual = g(truth) - g(fitted)}).
\item For each of \code{n_bootstrap} replicates, resample the link-scale residuals
with replacement, add them to the fitted values, back-transform to the
natural scale (so the synthetic targets respect the link's support), and
refit the base learner on a clone of the training task.
}

Each replicate is a full curve, so the replicates are draws of the \emph{mean}
curve \code{f(b)}. The point forecast \code{response} is their link-scale mean,
back-transformed (the predictive median under the link). The epistemic
\code{se_epistemic} is the link-scale spread of the replicate mean curves; the
total predictive \code{se} adds the aleatoric residual variance back. The
\code{samples}, \code{quantiles}, and \code{target_reached} predict types describe the
\emph{realised} future performance \code{y_b}: each draw is a replicate mean curve plus
a resampled link-scale residual, so they reflect both the bootstrap
mean-curve uncertainty and the residual noise.

Residual bootstrap is the natural choice here because every batch
contributes a single per-batch performance value: row-bootstrap of the
archive would simply reweight identical rows, while batch-bootstrap with
multiplicity would not be picked up by base learners that aggregate by
batch before fitting.

Creates a new bootstrap-wrapped LCE learner.
}
\section{Parameters}{

The bootstrap wrapper's own parameters are exposed alongside the base
learner's parameters via \link[paradox:ParamSetCollection]{paradox::ParamSetCollection}; the base learner's
parameters carry the \code{base.} prefix (e.g. \code{base.rate_lower}).

Own parameters:
\itemize{
\item \code{n_bootstrap} :: \code{integer(1)}\cr
Number of bootstrap replicates. Initialized to \code{100}.
\item \code{seed} :: \code{integer(1)} | \code{NULL}\cr
RNG seed for the residual resampling -- both the bootstrap refits at train
time and the realised-trajectory draws at predict time, so a fixed seed
makes the whole prediction reproducible. If \code{NULL} (default), the current
RNG state is used.
}
}

