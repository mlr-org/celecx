% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TaskLCE.R
\name{TaskLCE}
\alias{TaskLCE}
\title{Learning Curve Extrapolation Task}
\arguments{
\item{id}{(\code{character(1)})\cr
Task id.}

\item{backend}{(\link[mlr3:DataBackend]{mlr3::DataBackend} | \code{data.frame()})\cr
Data backend. Coerced via \code{\link[mlr3:as_data_backend]{mlr3::as_data_backend()}}.}

\item{target}{(\code{character(1)})\cr
Name of the numeric column holding the surrogate performance.}

\item{batch_nr}{(\code{character(1)})\cr
Name of the integer column holding the batch number. This column becomes
the task's single feature.}

\item{archive_x}{(\code{character()})\cr
Names of the archive feature columns (untransformed \code{x} values). At
least one column is required at construction.}

\item{archive_y}{(\code{character()})\cr
Names of the archive target columns (objective \code{y} values). At least
one column is required at construction.}

\item{search_space}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Search space of the originating optimization run. Its parameter ids must
match \code{archive_x}. Cloned and stored for replay extrapolators.}

\item{codomain}{(\link[bbotk:Codomain]{bbotk::Codomain})\cr
Codomain of the originating optimization run. Its target ids must match
\code{archive_y}. Cloned and stored for replay extrapolators.}

\item{measure}{(\link[mlr3:Measure]{mlr3::Measure} | \code{NULL})\cr
Regression measure that produced the performance \code{target} column. Cloned
and stored so replay extrapolators can score on the same scale. \code{NULL}
when the task carries no such provenance.}

\item{pool}{(\link[data.table:data.table]{data.table::data.table} | \code{NULL})\cr
Finite candidate pool of the originating run, for pool-based active
learning. Its columns must be exactly \code{archive_x} (the search-space
parameter ids). \code{NULL} for continuous search spaces.}

\item{link}{(\code{character(1)})\cr
Name of the predictive \link{lce_link} on whose scale the learners model the
performance curve and the distributional measures interpret \code{se}. A
property of the target metric; \code{"identity"} by default. See \link{lce_link}
(and \link{lce_link_from_range} to derive one from a measure's range).}

\item{label}{(\code{character(1)})\cr
Optional label.}

\item{extra_args}{(\code{list()})\cr
Extra constructor arguments preserved for cloning.}

\item{rows}{(\code{integer()})}
}
\value{
\code{numeric()}.

\code{\link[data.table:data.table]{data.table::data.table()}}.

\code{\link[data.table:data.table]{data.table::data.table()}}.
}
\description{
Task class for predicting the surrogate-model quality trajectory of an active
learning run. Each row corresponds to one archive evaluation and carries the
batch in which it was evaluated, the untransformed archive feature values,
the archive target values, and the surrogate's performance for that batch.

The task has a single feature, \code{batch_nr}, which is the only column passed to
a \link{LearnerLCE} at predict time. The archive feature and target columns are
carried in the dedicated column roles \code{archive_x} and \code{archive_y} so that
more sophisticated extrapolators may inspect them during training without
the columns being required for prediction on new data.

Optionally, the task also carries the \link[paradox:ParamSet]{paradox::ParamSet} search space and the
\link[bbotk:Codomain]{bbotk::Codomain} of the originating optimization run. These cannot be
recovered from the archive columns alone (which only retain storage types),
but they are required by extrapolators that replay the active-learning loop,
since they define parameter bounds, transformations, dependencies, and the
codomain optimization directions.

Two further pieces of run provenance can be carried for replay extrapolators:
the regression \link[mlr3:Measure]{mlr3::Measure} that produced the performance \code{target} column
(so a simulation can score on the same scale) and, for finite-pool active
learning, the candidate \code{pool} (so a simulation can propose from the same
candidate set). The originating surrogate is \emph{not} stored here, as it is a
property of the optimizer rather than of the data.

\code{TaskLCE} objects are typically constructed by tracking an \code{\link{OptimizerAL}}
optimization run with a \code{\link{CallbackSurrogatePerformance}} callback.

Creates a new LCE task.

True surrogate performance for the given rows (defaults to all active rows).

Returns the archive feature data (columns with role \code{archive_x}).

Returns the archive target data (columns with role \code{archive_y}).
}
\section{Fields}{

\describe{
\item{\code{batch_nr}}{(\code{character(1)})\cr
Name of the batch_nr column.}

\item{\code{archive_x}}{(\code{character()})\cr
Names of the archive feature columns.}

\item{\code{archive_y}}{(\code{character()})\cr
Names of the archive target columns.}

\item{\code{batch_nrs}}{(\code{integer()})\cr
Batch number for every active row, in the order of \code{task$row_ids}.}

\item{\code{search_space}}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Search space of the originating optimization run.}

\item{\code{codomain}}{(\link[bbotk:Codomain]{bbotk::Codomain})\cr
Codomain of the originating optimization run.}

\item{\code{measure}}{(\link[mlr3:Measure]{mlr3::Measure} | \code{NULL})\cr
Regression measure that produced the performance \code{target} column.}

\item{\code{pool}}{(\link[data.table:data.table]{data.table::data.table} | \code{NULL})\cr
Finite candidate pool of the originating run.}

\item{\code{link}}{(\code{character(1)})\cr
Name of the predictive link scale (see \link{lce_link}).}
}}

