% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring_functions.R
\name{scoring_functions}
\alias{scoring_functions}
\alias{scoring_random}
\alias{scoring_gsx}
\alias{scoring_gsy}
\alias{scoring_igs}
\alias{scoring_qbc}
\title{Scoring Functions for Pool-Based Active Learning}
\usage{
scoring_random()

scoring_gsx()

scoring_gsy()

scoring_igs()

scoring_qbc(k_qbc = 5L)
}
\arguments{
\item{k_qbc}{Number of bootstrap committee members (default 5).}
}
\description{
Factory functions that return scoring closures for use with \link{OptimizerGS}.
Each factory returns a function with signature:

\verb{function(pool_x, unqueried_idx, learner, queried_idx, queried_y, target_id, pool_x_raw = NULL)}

where:
\itemize{
\item \code{pool_x}: numeric matrix (N x d) of scaled features used for distance
computations
\item \code{unqueried_idx}: integer vector of unqueried pool indices
\item \code{learner}: fitted \link[mlr3:LearnerRegr]{mlr3::LearnerRegr} or \code{NULL}
\item \code{queried_idx}: integer vector of queried pool indices
\item \code{queried_y}: numeric vector of true labels for queried points
\item \code{target_id}: string, target column name
\item \code{pool_x_raw}: optional numeric matrix (N x d) of raw (unscaled) features.
When supplied, model-based scoring functions use these for learner
predictions instead of \code{pool_x}, keeping the learner's input contract
independent of the distance-scaling convention. When \code{NULL}, \code{pool_x} is
used for both distances and predictions (backward-compatible).
}

Returns a numeric vector of scores (length = \code{length(unqueried_idx)}),
where higher is better.
}
\section{Functions}{
\itemize{
\item \code{scoring_random()}: Random scores (baseline).

\item \code{scoring_gsx()}: GSx: maximin input-space distance. Model-free.

\item \code{scoring_gsy()}: GSy: maximin output-space distance.
Compares candidate predictions to selected true labels.

\item \code{scoring_igs()}: iGS: maximin joint input-output product.
The product is inside the min: \code{score(n) = min_m(d_x * d_y)}.

\item \code{scoring_qbc()}: QBC: committee disagreement via bootstrap.

}}
