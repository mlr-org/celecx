% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCE.R
\name{LearnerLCE}
\alias{LearnerLCE}
\title{Learning Curve Extrapolation Learner}
\arguments{
\item{id}{(\code{character(1)})\cr
Learner id.}

\item{param_set}{(\link[paradox:ParamSet]{paradox::ParamSet}).}

\item{predict_types}{(\code{character()})\cr
One or more of \code{"response"}, \code{"se"}, \code{"quantiles"}, \code{"samples"},
\code{"target_reached"}.}

\item{feature_types}{(\code{character()})\cr
Supported feature types. Defaults to \code{"integer"} (the type of \code{batch_nr}).}

\item{properties}{(\code{character()}).}

\item{packages}{(\code{character()}).}

\item{label}{(\code{character(1)}).}

\item{man}{(\code{character(1)}).}
}
\description{
Abstract base class for learners that extrapolate the surrogate-model quality
trajectory of an active-learning run. Subclasses fit a model on a \link{TaskLCE}
and produce point and (optionally) distributional predictions of the
surrogate's performance at unseen \code{batch_nr} values.

The supported predict types are:
\itemize{
\item \code{"response"}: the point forecast (predictive median on the natural scale).
\item \code{"se"}: additionally the total predictive standard error \code{se} and the
epistemic standard error of the mean \code{se_epistemic}, both on the task's
\link{lce_link} scale (see \link{PredictionLCE}).
\item \code{"quantiles"}: a matrix of predictive quantiles at the probabilities given
by the \code{quantile_probs} parameter.
\item \code{"samples"}: a matrix of predictive draws (joint sample paths).
\item \code{"target_reached"}: a matrix of reach probabilities at the targets given by
the \code{reach_target} parameter; needs the task's optimization direction (so
the task must carry a \code{measure}).
}

Subclasses that model the curve as Gaussian on the link scale assemble their
predictions with \code{lce_distr_predict()}; subclasses that produce explicit
predictive draws use \code{lce_samples_predict()}.

Creates a new LCE learner.
}
\section{Parameters}{

Depending on the supported predict types, the following parameters are added
automatically:
\itemize{
\item \code{quantile_probs} :: \code{numeric()}\cr
Probabilities for the \code{"quantiles"} predict type. Defaults to
\code{c(0.05, 0.25, 0.5, 0.75, 0.95)}.
\item \code{reach_target} :: \code{numeric()}\cr
Target value(s) for the \code{"target_reached"} predict type. No default.
}
}

