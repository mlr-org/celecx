% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCESplineMonotone.R
\name{mlr_learners_lce.spline_monotone}
\alias{mlr_learners_lce.spline_monotone}
\alias{LearnerLCESplineMonotone}
\title{Monotone Spline LCE Learner}
\description{
Fits a shape-constrained smoothing spline (monotone increasing or monotone
decreasing) to the per-batch surrogate performance via
\code{\link[scam:scam]{scam::scam()}} with a monotone P-spline basis (\code{"mpi"} / \code{"mpd"}).
With \code{direction = "auto"} (the default), the direction is inferred from the
task's measure: utility-style targets increase and loss-style targets
decrease.
This is the smoother, lower-bias analogue of \link{LearnerLCEIsotonic}.

Extrapolation beyond the training-batch range linearly extends the
last fitted spline segment, so very long-horizon forecasts can drift; use
\link{LearnerLCEParametricExponential} or one of the parametric families when
a hard asymptote is needed. The spline is fit on the task's \link{lce_link} scale.

At least five distinct batches are required: \code{scam}'s monotone basis needs a
basis dimension \code{k} of at least 4, and \code{k} is clamped to at most
\code{n_batches - 1} to keep the fit identifiable.

When \code{predict_type = "se"} the learner returns the spline's pointwise standard
errors (as reported by \code{\link[scam:predict.scam]{scam::predict.scam()}}) as the epistemic \code{se_epistemic}
and adds the spline's residual variance back to form the total predictive
\code{se}, both on the link scale.

Creates a new instance of this learner.
}
\section{Parameters}{

\itemize{
\item \code{direction} :: \code{character(1)}\cr
\code{"auto"} (default), \code{"increasing"}, or \code{"decreasing"}. Selects the monotone
P-spline basis (\code{mpi} / \code{mpd}) after resolution.
\item \code{k} :: \code{integer(1)} | \code{NULL}\cr
Basis dimension passed to \code{scam::s()}. Defaults to \code{NULL}, which uses
\code{min(10, n_batches - 1)}. Floored at \code{4} (the smallest \code{k} the monotone
basis supports) and clamped to at most \code{n_batches - 1} at training time to
keep the fit identifiable.
\item \code{bs} :: \code{character(1)}\cr
Spline basis. Restricted to \code{"mpi"} (monotone increasing) and \code{"mpd"}
(monotone decreasing) and is set automatically from \code{direction}.
}
}

