% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CallbackSurrogatePerformance.R
\name{celecx.surrogate_performance}
\alias{celecx.surrogate_performance}
\alias{CallbackSurrogatePerformance}
\title{Surrogate Performance Callback}
\arguments{
\item{surrogate_id}{(\code{character(1)})\cr
Surrogate registry id in \code{optimizer$surrogates}.}

\item{task}{(\link[mlr3:TaskRegr]{mlr3::TaskRegr})\cr
Held-out regression task.}

\item{measures}{(\code{list()} of \link[mlr3:Measure]{mlr3::Measure})\cr
Regression measures. Named lists use their names as output column names;
unnamed measures use their measure ids.}

\item{measure}{(\code{character(1)} | \code{NULL})\cr
Name of the measure column to use as task target. Refers to the
\verb{$state$measures} names. When \code{NULL} (default) and the callback was
configured with exactly one measure, that measure is used.}

\item{id}{(\code{character(1)})\cr
Task id. Defaults to \code{"surrogate_performance"}.}

\item{link}{(\code{character(1)})\cr
Name of the predictive \link{lce_link} for the resulting \link{TaskLCE}.
\code{"identity"} by default.}

\item{label}{(\code{character(1)})\cr
Optional task label.}
}
\value{
\link{TaskLCE}.
}
\description{
Evaluates a named \link{OptimizerAL} surrogate on a held-out regression task after
every evaluated batch.

The callback stores one row per batch in \verb{$data}. Rows include the archive
batch number, cumulative number of evaluations, timestamp, surrogate id, and
one column per configured regression measure.

Creates a new CallbackSurrogatePerformance.

Clears the logged performance history and the stored archive reference.

Build a \link{TaskLCE} from the recorded archive and surrogate performance.

The resulting task has one row per archive evaluation. Its columns are
the archive search-space columns (role \code{archive_x}), the archive
codomain target columns (role \code{archive_y}), the \code{batch_nr} column
(role \code{feature}), and the selected per-batch surrogate-performance
column (role \code{target}). The archive's search space and codomain, the
selected regression measure, and (for pool-based runs) the candidate
pool are carried along for extrapolators that replay the active-learning
loop.
}
\section{Fields}{

\describe{
\item{\code{data}}{(\code{data.table})\cr
Logged performance history.}

\item{\code{latest}}{(\code{data.table} | \code{NULL})\cr
Most recently logged row.}
}}

\examples{
\dontrun{
perf <- clbk("celecx.surrogate_performance",
  surrogate_id = "uncertainty",
  task = test_task,
  measures = list(r2 = msr("regr.rsq"), mae = msr("regr.mae"))
)
}
}
