% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCERollingSlope.R
\name{mlr_learners_lce.rolling_slope}
\alias{mlr_learners_lce.rolling_slope}
\alias{LearnerLCERollingSlope}
\title{Rolling Slope LCE Learner}
\description{
Baseline \link{LearnerLCE} that fits a straight line through the most recent
\code{window} per-batch training performances and linearly extrapolates it to
the requested test batches.

This is a stronger "no-curvature" baseline than the constant predictors of
\link{LearnerLCEFeatureless} and matches the "rolling slope" baseline from the
celecx research plan. The line is fit on the task's \link{lce_link} scale.

When \code{predict_type = "se"}, the standard errors come from the ordinary
least-squares fit: \code{se_epistemic} is the SD of the fitted mean (so it \emph{grows}
as the requested batch moves away from the window, reflecting extrapolation
uncertainty) and \code{se} adds the residual variance back to form the total
predictive SD. Both are on the link scale and are \code{NA} when fewer than three
window batches make the residual variance undefined.

Creates a new instance of this learner.
}
\section{Parameters}{

\itemize{
\item \code{window} :: \code{integer(1)}\cr
Number of most recent batches used for the slope fit. Initialized to \code{5}.
The learner uses \code{min(window, n_training_batches)} batches.
}
}

