% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCEExtrapolator.R
\name{LearnerLCEExtrapolator}
\alias{LearnerLCEExtrapolator}
\title{Learning Curve Extrapolation Learner via CurveExtrapolator}
\arguments{
\item{extrapolator}{(\link{CurveExtrapolator})\cr
The extrapolator to wrap.}

\item{id}{(\code{character(1)} | \code{NULL})\cr
Optional identifier. Defaults to a name derived from the
extrapolator class.}
}
\description{
Wraps a \link{CurveExtrapolator} instance as an mlr3 \link{LearnerLCE}, enabling
the standard \code{resample()} / \code{benchmark()} workflow for evaluating
learning curve extrapolation methods.

Creates a new LearnerLCEExtrapolator.
}
\details{
During training, the order column and target are extracted from the
\link{TaskLCE} and passed to the extrapolator's \verb{$train()} method.
Other feature columns are ignored (\code{"featureless"} property).

During prediction, the order column values are extracted and passed to
the extrapolator's \verb{$predict()} method, returning point estimates
and/or quantiles.
}
\section{Fields}{

\describe{
\item{\code{extrapolator}}{(\link{CurveExtrapolator})
The wrapped extrapolator (read-only template).}

\item{\code{param_set}}{(\link[paradox:ParamSet]{paradox::ParamSet})
Combined parameter set.}
}}

\section{Predict Types}{

\itemize{
\item \code{"response"}: Returns the median forecast (\code{q50}).
\item \code{"se"}: Returns \code{q50} as response and a standard error derived from
the normal approximation to the 90\% prediction interval.
\item \code{"quantiles"}: Returns quantile forecasts at levels 0.05, 0.50, 0.95.
}
}

