% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CallbackSurrogatePerformance.R
\name{task_lce_from_perf}
\alias{task_lce_from_perf}
\title{Build an LCE Task from an Archive and a Performance Table}
\usage{
task_lce_from_perf(
  archive,
  perf,
  measure,
  measure_obj = NULL,
  pool = NULL,
  link = "identity",
  id,
  label = NA_character_
)
}
\arguments{
\item{archive}{(\link[bbotk:Archive]{bbotk::Archive})\cr
Archive of the originating run; supplies the \code{archive_x} / \code{archive_y}
columns, \code{batch_nr}, and the search space / codomain provenance.}

\item{perf}{(\link[data.table:data.table]{data.table::data.table})\cr
Per-batch performance table with a \code{batch_nr} column plus one column per
measure. Archive batches without a matching \code{perf} row are dropped.}

\item{measure}{(\code{character(1)})\cr
Name of the \code{perf} column to use as task target.}

\item{measure_obj}{(\link[mlr3:Measure]{mlr3::Measure} | \code{NULL})\cr
The regression measure that produced the target column; carried into the
task so replay extrapolators can score on the same scale.}

\item{pool}{(\link[data.table:data.table]{data.table::data.table} | \code{NULL})\cr
Candidate pool of the originating run (search-space columns only), for
pool-restricted replay.}

\item{link}{(\code{character(1)})\cr
Name of the predictive \link{lce_link} for the resulting task.}

\item{id}{(\code{character(1)})\cr
Task id.}

\item{label}{(\code{character(1)})\cr
Optional task label.}
}
\value{
\link{TaskLCE}.
}
\description{
Builds a \link{TaskLCE} by joining an optimization archive with a per-batch
performance table: one task row per archive evaluation, with the selected
performance column as target. This is the assembly used by
\link{CallbackSurrogatePerformance}\verb{$task()} and
\code{\link[=replay_surrogate_performance]{replay_surrogate_performance()}}; it is exported for benchmark harnesses
that record archives and curves separately and rebuild tasks later.
}
