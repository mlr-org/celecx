% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ObjectiveLearner.R
\name{ObjectiveLearner}
\alias{ObjectiveLearner}
\title{Objective Function Based on a Fitted Learner}
\arguments{
\item{learner}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr})\cr
A fitted regression learner. Must have been trained via \verb{$train()}
before creating the objective.}

\item{domain}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Parameter set describing the input space. All parameter IDs must
correspond to features the learner was trained on.}

\item{codomain}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Parameter set describing the output space. Must contain at least one
target tagged with \code{"minimize"}, \code{"maximize"}, or \code{"learn"}.}

\item{id}{(\code{character(1)})\cr
Identifier for the objective.}

\item{check_values}{(\code{logical(1)})\cr
Whether to check validity of input configurations against the domain.}

\item{xdt}{(\code{data.table})\cr
A data.table with one configuration per row.}
}
\value{
\code{\link[data.table:data.table]{data.table::data.table()}} containing codomain columns.
}
\description{
An \link[bbotk:Objective]{bbotk::Objective} subclass where evaluation uses a fitted regression learner
to predict outcomes. This is useful for surrogate-based optimization where
a model has been trained on observed data and we want to optimize over
predicted outcomes.

Creates a new instance of this \link[R6:R6Class]{R6} class.

Evaluates multiple input values on the objective function.
}
\details{
The learner must be trained before creating the objective. The domain
parameters must correspond to features the learner was trained on.
For factor features, domain levels must be a subset of the levels
present in the training data (the learner cannot generalize to unseen
factor levels).

Type compatibility:
\itemize{
\item \code{ParamDbl} corresponds to numeric training features
\item \code{ParamInt} corresponds to integer or numeric training features
\item \code{ParamFct} corresponds to factor training features (domain levels must be subset)
\item \code{ParamLgl} corresponds to logical training features
}
}
\section{Fields}{

\describe{
\item{\code{learner}}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr})\cr
Read-only access to the internal learner.}

\item{\code{train_task}}{(\link[mlr3:TaskRegr]{mlr3::TaskRegr})\cr
Returns the task the learner was trained on (without data backend).}
}}

