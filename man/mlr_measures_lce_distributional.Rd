% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MeasureLCEDistributional.R
\name{mlr_measures_lce_distributional}
\alias{mlr_measures_lce_distributional}
\title{Distributional LCE Measures}
\description{
Per-batch distributional measures for \link{TaskLCE} / \link{LearnerLCE} evaluations.
Each measure aggregates the prediction and truth to one value per batch and
then scores the per-batch predictive distribution against the realised
performance. The predictive is interpreted as Gaussian on the task's
\link{lce_link} scale, carried by the \code{se} predict type, except for \code{lce.pinball},
which reads the \code{quantiles} predict type.
\itemize{
\item \code{lce.crps}: closed-form continuous ranked probability score of the
Normal-on-link predictive against the link-transformed truth. The headline
proper score for the forecast distribution.
\item \code{lce.reach_brier}: Brier score of the predicted probability that the metric
has reached \code{target} against whether the realised per-batch performance has.
The direction (reach from above / below) is read from the task's measure.
When the prediction carries a \code{target_reached} column for the requested
\code{target}, that probability is scored directly (exact, draw-based for the
sample-based learners); otherwise the probability is the Gaussian-on-link
form computed from \code{response} / \code{se}. This is the proper, grid-free way to
benchmark "batches-to-target".
\item \code{lce.coverage}: empirical coverage of the central \code{level} predictive
interval (ideally equal to \code{level}; reported, not optimised).
\item \code{lce.interval_score}: Winkler interval score of the central \code{level}
predictive interval (sharpness plus miscoverage penalty).
\item \code{lce.pinball}: average pinball (quantile) loss at quantile \code{alpha}, which
must be one of the probabilities the learner predicted. Unlike the other
distributional measures it is scored on the natural scale, since it consumes
the natural-scale \code{quantiles} predict type directly.
}

All measures support observation weights from a \code{weights_measure} task column;
the weight of a batch is the sum of weights of its archive rows.
}
