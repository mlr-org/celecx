% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OptimizerIDEAL.R
\name{mlr_optimizers_ideal}
\alias{mlr_optimizers_ideal}
\alias{OptimizerIDEAL}
\title{IDEAL Active Learning Optimizer}
\arguments{
\item{learner}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr})\cr
Regression learner used for predictions.}

\item{continuous_optimizer}{(\code{function} | \code{NULL})\cr
Optional acquisition optimizer for population mode.}
}
\description{
Active learning optimizer implementing the IDEAL algorithm
(Active Learning by Inverse Distance Weighting) from Alberto Bemporad,
"Active Learning for Regression by Inverse Distance Weighting". Uses model-
external uncertainty estimation via IDW-weighted residuals plus an
exploration term.

Features are affine-scaled to \eqn{[-1, 1]} for the IDEAL geometry, while
the regression learner is trained and queried on the raw feature values.

Pool mode is available for \link{ObjectiveDataset} objectives. Population mode is
available for bounded numeric objectives and optimizes the acquisition with
either an injected \code{continuous_optimizer} or a PSO-based default.

The optimizer targets \link{SearchInstance} directly and is not routed through the
MBO acquisition function pipeline.

Creates a new instance of this \link[R6:R6Class]{R6} class.
}
\section{Fields}{

\describe{
\item{\code{learner}}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr})\cr
Regression learner.}

\item{\code{continuous_optimizer}}{(\code{function} | \code{NULL})\cr
Optional population-mode acquisition optimizer.}
}}

\section{Parameters}{

\itemize{
\item \code{delta} (\code{numeric(1)})\cr
Exploration-exploitation tradeoff. Default 1.
\item \code{n_init} (\code{integer(1)})\cr
Number of initial samples required. Default: dimension of search space.
\item \code{init_method} (\code{character(1)})\cr
Initialization strategy for pool mode: \code{"kmeans"} (default, per the IDEAL
paper), \code{"gsx"} (centroid-nearest + farthest-first, per the GS paper), or
\code{"random"}. Population mode always uses LHS regardless of this setting.
\item \code{batch_size} (\code{integer(1)})\cr
Points per iteration. Candidates within a batch are selected sequentially
with the IDW geometry updated after each provisional pick, and the model
is refit after the whole batch has been evaluated.
}
}

\section{Construction}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{OptimizerIDEAL$new(learner, continuous_optimizer = NULL)
}\if{html}{\out{</div>}}
\itemize{
\item \code{learner} (\link[mlr3:LearnerRegr]{mlr3::LearnerRegr})\cr
Regression learner used for predictions.
\item \code{continuous_optimizer} (\code{function} | \code{NULL})\cr
Optional acquisition optimizer used in population mode. It must have
signature \verb{function(search_space, score_function)} where \code{score_function}
maps a numeric candidate vector to a scalar acquisition value. The return
value must describe a single candidate as a named numeric vector, named
list, or one-row \code{data.table}. If \code{NULL}, population mode uses
\code{pso::psoptim()}.
}

Or use the convenience constructor \code{\link[=optimizer_pool_al]{optimizer_pool_al()}} for pool mode.
}

\section{State Persistence}{

After optimization, the attempt history is persisted in
\code{archive$data_extra[["OptimizerIDEAL"]]} for replay compatibility
with \link{CurveExtrapolatorSim}.
}

