% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OptimizerPoolSample.R
\name{mlr_optimizers_pool_sample}
\alias{mlr_optimizers_pool_sample}
\alias{OptimizerPoolSample}
\title{Sampler-Based Search on Pool-Restricted or Discrete Objectives}
\arguments{
\item{space_sampler}{(\link{SpaceSampler})\cr
Sampler used to draw each batch. Default is \link{SpaceSamplerUniform}.}

\item{grid_expansion_limit}{(\code{integer(1)})\cr
Upper limit on the number of rows to materialize for the full grid for
discrete search spaces in Objectives that are not pool-restricted.
An error is raised if the full grid would exceed this limit.
This is to protect against accidental usage of too much memory.
Default is \code{1e7L}.}
}
\description{
Optimizer that samples configurations with a \link{SpaceSampler} from a candidate
pool when the objective has the \code{"pool_restricted"} property (typically by
inheriting from \link{ObjectivePoolAbstract}), or from the full grid of a
completely discrete search space (where all parameters are \link[paradox:Domain]{paradox::p_int},
\link[paradox:Domain]{paradox::p_fct}, or \link[paradox:Domain]{paradox::p_lgl}).

When the search space is continuous (not fully discrete) and the objective is
not pool-restricted, this optimizer samples directly from the search space.

Creates a new instance of this \link[R6:R6Class]{R6} class.
}
\section{Fields}{

\describe{
\item{\code{space_sampler}}{(\link{SpaceSampler})\cr
Wrapped sampler.}

\item{\code{param_set}}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Combined parameter set of this optimizer and the wrapped sampler.}
}}

\section{Parameters}{

\describe{
\item{\code{batch_size}}{\code{integer(1)}\cr
Number of configurations per batch. \code{Inf} evaluates the entire remaining
pool in a single batch when \code{replace_samples = "never"}, or the full pool
in each batch when \code{replace_samples = "between_batches"}. \code{Inf} is
forbidden when the objective is not pool-restricted and the search space is
not fully discrete. Default is \code{1}.}
\item{\code{max_batches}}{\code{integer(1)}\cr
Maximum number of batches to evaluate. \code{Inf} means no batch limit
(termination is governed solely by the instance's terminator). Default is
\code{Inf}.}
\item{\code{replace_samples}}{\code{character(1)}\cr
Controls duplicate handling across evaluations:
\itemize{
\item \code{"never"}: configurations already present in the archive are
excluded from sampling. The pool shrinks as evaluations accumulate.
\item \code{"between_batches"}: each batch is sampled from the full pool, but
previous batches do not affect future sampling.
}
Default is \code{"never"}.}
\item{\code{pass_known_pool}}{\code{logical(1)}\cr
Whether evaluated configurations from the archive are passed to the
\link{SpaceSampler}'s \code{sample()} method as \code{known_pool}. Default is \code{TRUE}.}
}

The wrapped sampler's parameters are exposed with a \code{space_sampler.} prefix.
}

