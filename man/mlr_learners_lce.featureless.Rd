% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCEFeatureless.R
\name{mlr_learners_lce.featureless}
\alias{mlr_learners_lce.featureless}
\alias{LearnerLCEFeatureless}
\title{Featureless LCE Learner}
\description{
Baseline \link{LearnerLCE} which ignores the \code{batch_nr} value and predicts a
constant performance for every future batch. The constant is one of three
summaries of the per-batch training performances, selected via \code{type}:
\itemize{
\item \code{"average"}: the mean (or median, when \code{robust = TRUE}) of the considered
training batches. The "no learning-curve information used at all"
sanity-check baseline.
\item \code{"best"}: the best performance observed among the considered training
batches. The "no progress beyond the best we have already seen" baseline.
The optimization direction (whether higher or lower is better) is taken
from the task's \code{measure}, so the task must carry one for this \code{type}.
\item \code{"last"}: the performance of the most recent batch. The "no progress from
here" baseline.
}

The \code{window} parameter restricts "the considered batches" to the most recent
\code{window} of them (all of them by default). Location and dispersion are
computed on the task's \link{lce_link} scale. When \code{predict_type = "se"},
\code{se_epistemic} is the (robust) link-scale dispersion of the considered
per-batch performances divided by the square root of their count (the standard
error of the constant), and the total predictive \code{se} adds that dispersion
back as the aleatoric spread (\code{se = sqrt(dispersion^2 + se_epistemic^2)}), both
replicated for every prediction. For \code{type = "average"} this is the textbook
predictive standard deviation of a new observation; for \code{type = "best"} /
\code{"last"} the constant is not the window mean, so the symmetric interval it
implies is only a coarse heuristic. The uncertainty reflects nothing beyond
the spread of the observed curve.

Creates a new instance of this learner.
}
\section{Parameters}{

\itemize{
\item \code{type} :: \code{character(1)}\cr
\code{"average"}, \code{"best"}, or \code{"last"}. Initialized to \code{"best"}.
\item \code{robust} :: \code{logical(1)}\cr
When \code{TRUE}, use median + MAD instead of mean + SD. Affects the \code{"average"}
location and the dispersion of all types. Initialized to \code{FALSE}.
\item \code{window} :: \code{integer(1)}\cr
Number of most recent batches considered, clamped to the number of training
batches. \code{Inf} (the default) uses all batches. For \code{type = "last"} only the
\code{se} is affected, since the location is always the most recent batch.
}
}

