% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ResamplingLCE.R
\name{mlr_resamplings_lce.expanding_cv}
\alias{mlr_resamplings_lce.expanding_cv}
\alias{ResamplingLCE}
\title{Expanding-Window LCE Cross-Validation}
\description{
Time-series style cross-validation for \link{TaskLCE}. Splits are made by whole
batches (column with role \code{feature}, i.e. the \code{batch_nr} column of the task).
Each fold trains on all batches from the very first up to a moving training
end and tests on the next \code{horizon} batches; the training window therefore
always starts at the beginning and expands by \code{step_size} batches between
folds.

Creates a new expanding-window LCE resampling.
}
\section{Fields}{

\describe{
\item{\code{iters}}{(\code{integer(1)})\cr
Number of resampling iterations. Only meaningful after \verb{$instantiate()}.}
}}

\section{Parameters}{

\itemize{
\item \code{horizon} :: \code{integer(1)}\cr
Number of consecutive batches used as test set in every fold. Initialized
to \code{1}.
\item \code{step_size} :: \code{integer(1)}\cr
Number of batches between the train-end indices of consecutive folds.
Initialized to \code{1}.
\item \code{min_train_batches} :: \code{integer(1)}\cr
Number of batches in the training set of the first fold. Required, with no
default: results are sensitive to it and there is no good universal value.
Must be at least the wrapped learner's minimum training requirement, which
is larger for some learners (e.g. \link{LearnerLCESplineMonotone} needs five
batches, \link{LearnerLCEConformal} needs \code{n_calibration_batches + 1}).
\item \code{folds} :: \code{integer(1)} | \code{NULL}\cr
Maximum number of folds. When unset, all feasible folds are generated.
}
}

