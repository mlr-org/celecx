% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCEParametricExponential.R
\name{mlr_learners_lce.parametric_exponential}
\alias{mlr_learners_lce.parametric_exponential}
\alias{LearnerLCEParametricExponential}
\title{Parametric Exponential LCE Learner}
\description{
Fits a three-parameter exponential learning curve
\deqn{f(b) = c + a \exp(-k\,b)}
to the per-batch surrogate performance. Suitable both for performance
measures that grow to an asymptote (\code{a} ends up negative) and for loss
measures that decay to an asymptote (\code{a} ends up positive).

The curve is fit on the task's \link{lce_link} scale, so for a non-identity link
the family above describes \code{g(f(b))} and the natural-scale prediction is
\code{g^{-1}} of the fitted curve.

When \code{predict_type = "se"} the learner reports two link-scale standard errors:
\code{se_epistemic}, the Gauss-Newton delta-method SD of the mean curve (parameter
covariance \eqn{\hat\sigma^2 (H/2)^{-1}} propagated through the gradient of
\code{f}), and \code{se}, the total predictive SD \code{sqrt(se_epistemic^2 + sigma2)} that
adds the residual variance back. If the Hessian is singular or the residual
degrees of freedom are non-positive, both are \code{NA}.

Multiple archive rows belonging to the same batch are collapsed via the
batch-wise mean of \code{target} before fitting; ties always agree by
construction when the upstream task was produced by
\link{CallbackSurrogatePerformance}.

Creates a new instance of this learner.
}
\section{Parameters}{

\itemize{
\item \code{asymptote_init}, \code{amplitude_init}, \code{rate_init} :: \code{numeric(1)}\cr
Initial values for \code{c}, \code{a}, and \code{k}. Defaults derived from the training
data when unset.
\item \code{rate_lower} :: \code{numeric(1)}\cr
Lower bound for the decay rate \code{k}. Initialized to \code{1e-6} so the curve
stays monotone and the model identifiable.
\item \code{maxit} :: \code{integer(1)}\cr
Maximum optim iterations. Initialized to \code{500}.
}
}

