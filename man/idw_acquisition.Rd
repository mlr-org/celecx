% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idw_geometry.R
\name{idw_acquisition}
\alias{idw_acquisition}
\title{Compute IDEAL acquisition values for candidate pool points.}
\usage{
idw_acquisition(
  candidates_scaled,
  candidates_yhat,
  attempted_x_scaled,
  attempted_y,
  Q,
  delta,
  target_scale = 1,
  eps_eq = 1e-12
)
}
\arguments{
\item{candidates_scaled}{(\code{matrix})\cr
M x d matrix of scaled candidate points.}

\item{candidates_yhat}{(\code{numeric(M)})\cr
Model predictions for candidate points.}

\item{attempted_x_scaled}{(\code{matrix})\cr
K x d matrix of all evaluated points (scaled).}

\item{attempted_y}{(\code{numeric(K)})\cr
Targets for evaluated points (may contain NA for failures).}

\item{Q}{(\code{integer})\cr
Vector of indices into evaluated points for successful queries.}

\item{delta}{(\code{numeric(1)})\cr
Exploration weight (>= 0).}

\item{target_scale}{(\code{numeric(1)})\cr
Scaling factor for residuals. Default 1.}

\item{eps_eq}{(\code{numeric(1)})\cr
Tolerance for exact-match detection.}
}
\value{
\code{numeric(M)}: Acquisition values.
}
\description{
For each candidate, computes:
\deqn{a(x) = \sum_{k \in Q} v_k(x) (y_k - \hat{y}(x))^2 + \delta z(x)}
}
\details{
where \eqn{v_k} are normalized IDW weights over all attempted points
and the uncertainty sum uses only successful points in Q.
}
\keyword{internal}
