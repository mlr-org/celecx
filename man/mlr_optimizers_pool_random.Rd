% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OptimizerPoolRandom.R
\name{mlr_optimizers_pool_random}
\alias{mlr_optimizers_pool_random}
\alias{OptimizerPoolRandom}
\title{Random Search on Pool-Restricted or Discrete Objectives}
\arguments{
\item{grid_expansion_limit}{(\code{integer(1)})\cr
Upper limit on the number of rows to materialize for the full grid for
discrete search spaces in Objectives that are not pool-restricted.
An error is raised if the full grid would exceed this limit.
This is to protect against accidental usage of too much memory.
Default is \code{1e7L}.}
}
\description{
Optimizer that randomly samples configurations from a candidate pool when
the objective has the \code{"pool_restricted"} property (typically by inheriting from
\link{ObjectivePoolAbstract}), or from the full grid of a completely discrete
search space (where all parameters are \link[paradox:Domain]{paradox::p_int}, \link[paradox:Domain]{paradox::p_fct},
or \link[paradox:Domain]{paradox::p_lgl}).

When the search space is continuous (not fully discrete) and the objective is
not pool-restricted, this optimizer falls back to uniform random search
(like \link[bbotk:mlr_optimizers_random_search]{bbotk::OptimizerBatchRandomSearch}).

Creates a new instance of this \link[R6:R6Class]{R6} class.
}
\section{Parameters}{

\describe{
\item{\code{batch_size}}{\code{integer(1)}\cr
Number of configurations per batch. \code{Inf} evaluates the entire remaining
pool in a single batch (forbidden when \code{replace_samples = "within_batches"}
or when the objective is not pool-restricted).
Default is \code{1}.}
\item{\code{max_batches}}{\code{integer(1)}\cr
Maximum number of batches to evaluate. \code{Inf} means no batch limit
(termination is governed solely by the instance's terminator). Default is
\code{Inf}.}
\item{\code{replace_samples}}{\code{character(1)}\cr
Controls duplicate handling across evaluations:
\itemize{
\item \code{"never"}: configurations already present in the archive are
excluded from sampling. The pool shrinks as evaluations accumulate.
\item \code{"between_batches"}: each batch is drawn without replacement from
the full pool, but previous batches do not affect future sampling.
\item \code{"within_batches"}: samples inside each batch are drawn with
replacement from the full pool, so duplicates are possible even
within a single batch.
}
Default is \code{"never"}.}
}
}

