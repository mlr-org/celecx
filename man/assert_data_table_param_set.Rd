% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_objective.R
\name{assert_data_table_param_set}
\alias{assert_data_table_param_set}
\title{Validate \code{data.table} Against \code{ParamSet}}
\usage{
assert_data_table_param_set(
  dt,
  param_set,
  require_uniqueness = TRUE,
  min_rows = 1L,
  presence = "all",
  allow_untyped = FALSE,
  allow_extra = TRUE,
  .param_set_name = "param_set",
  .dt_name = "dt"
)
}
\arguments{
\item{dt}{(\code{data.table})\cr
\code{data.table} to validate.}

\item{param_set}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
\code{ParamSet} describing the space to validate against.}

\item{require_uniqueness}{(\code{logical(1)})\cr
Whether to require uniqueness within the space. Default is \code{TRUE}.}

\item{min_rows}{(\code{integer(1)})\cr
Minimum number of rows required in \code{dt}. Default is \code{1L}.}

\item{presence}{(\code{character(1)})\cr
Whether all parameters must be present as columns in \code{dt}.
Use \code{"all"} (default) to require all parameters, or \code{"subset"} to allow
missing parameter columns and validate them as typed \code{NA} values.}

\item{allow_untyped}{(\code{logical(1)})\cr
Whether to allow \code{p_uty} in the \code{ParamSet}. Default is \code{FALSE}.
\code{p_uty} columns are not checked for type compatibility. Their
\code{custom_check} is applied when present.}

\item{allow_extra}{(\code{logical(1)})\cr
Whether columns outside the \code{ParamSet} are allowed. Default is \code{TRUE}.}

\item{.param_set_name}{(\code{character(1)})\cr
Name of the \code{ParamSet} for error messages. Default is \code{"param_set"}.}

\item{.dt_name}{(\code{character(1)})\cr
Name of the \code{data.table} for error messages. Default is \code{"dt"}.}
}
\value{
The validated and possibly coerced \code{data.table} (modified by reference).
}
\description{
Validates that a \code{data.table} is compatible with a given \code{ParamSet}.
Checks column presence, types, bounds, and uniqueness within the domain and
does type coercion if necessary.
}
\details{
Extra columns are allowed and are passed through unchanged when
\code{allow_extra = TRUE}.
Missing parameter columns are rejected unless \code{presence = "subset"}, in
which case they are validated as typed \code{NA} values on an internal copy.
When \code{require_uniqueness} is \code{TRUE}, tables with rows that are duplicated
within the space of the \code{ParamSet} are rejected, even if they differ by other columns.
}
\keyword{internal}
