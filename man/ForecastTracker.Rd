% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ForecastTracker.R
\name{ForecastTracker}
\alias{ForecastTracker}
\title{Forecast Tracker}
\arguments{
\item{extrapolator}{(\code{CurveExtrapolator})
Extrapolator used for forecasting.}

\item{metric_name}{(\code{character(1)})
Metric column in incoming metrics data.}

\item{x_col}{(\code{character(1)})
Evaluation-count column in incoming metrics data.}

\item{direction}{(\code{character(1)} | \code{NULL})
Direction override. If \code{NULL}, uses \code{extrapolator$direction}.}

\item{target}{(\code{numeric(1)} | \code{NULL})
Optional target value.}

\item{n_evals_budget}{(\code{integer(1)} | \code{NULL})
Optional budget horizon.}

\item{min_points}{(\code{integer(1)})
Minimum number of rows required before fitting.}

\item{update_every}{(\code{integer(1)})
Update period in rows.}

\item{default_horizon}{(\code{integer(1)})
Additional horizon when \code{n_evals_budget} is \code{NULL}.}

\item{training_args}{(\code{list})
Extra arguments passed to \code{extrapolator$train()}.}

\item{metrics_data}{(\code{data.table})
Metrics history table.}

\item{clear_extrapolator}{(\code{logical(1)})
Whether to also clear the extrapolator state.}

\item{...}{(ignored).}
}
\value{
Invisibly returns the appended row or \code{NULL} if no update happened.
}
\description{
Tracks forecast updates from a \code{CurveExtrapolator} during a run.

Creates a new ForecastTracker.

Update forecast state from metrics history.

Set/replace training arguments for future updates.

Clear forecast history.

Printer.
}
\details{
A \code{ForecastTracker} consumes a metrics history table (typically from
\link{MetricsTracker}) and periodically retrains its extrapolator, then stores the
resulting forecast summaries and prediction curves.
}
\section{Fields}{

\describe{
\item{\code{extrapolator}}{(\code{CurveExtrapolator})}

\item{\code{metric_name}}{(\code{character(1)})}

\item{\code{x_col}}{(\code{character(1)})}

\item{\code{direction}}{(\code{character(1)})}

\item{\code{target}}{(\code{numeric(1)} | \code{NULL})}

\item{\code{n_evals_budget}}{(\code{integer(1)} | \code{NULL})}

\item{\code{data}}{(\code{data.table})
Forecast history table.}

\item{\code{latest}}{(\code{data.table} | \code{NULL})
Most recent forecast update.}

\item{\code{n_updates}}{(\code{integer(1)})
Number of forecast updates.}

\item{\code{min_points}}{(\code{integer(1)})
Minimum rows needed before updates start.}

\item{\code{update_every}}{(\code{integer(1)})
Update period.}
}}

