% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCEGam.R
\name{mlr_learners_lce.gam}
\alias{mlr_learners_lce.gam}
\alias{LearnerLCEGam}
\title{GAM LCE Learner}
\description{
Fits an unconstrained smoothing spline to the per-batch performance via
\code{\link[mgcv:gam]{mgcv::gam()}} with a single smooth \code{s(batch_nr, k = k, bs = bs)}, estimated
by REML. This is the classic "GAM extrapolation with confidence bands"
workflow for learning-curve extrapolation. Unlike
\link{LearnerLCESplineMonotone} it imposes no monotonicity, so it can also track
non-monotone curves -- at the price of less stable extrapolation.

The curve is fit on the task's \link{lce_link} scale. Extrapolation beyond the
training-batch range extends the spline basis (linearly beyond the boundary
knots for the \code{"cr"} basis), so long-horizon forecasts can drift; the
pointwise standard errors grow accordingly.

When \code{predict_type = "se"} the learner returns the spline's pointwise
standard errors (as reported by \code{\link[mgcv:predict.gam]{mgcv::predict.gam()}}) as the epistemic
\code{se_epistemic} and adds the residual variance back to form the total
predictive \code{se}, both on the link scale.

At least four distinct batches are required; \code{k} is clamped to at most
\code{n_batches - 1} to keep the fit identifiable.

Creates a new instance of this learner.
}
\section{Parameters}{

\itemize{
\item \code{k} :: \code{integer(1)} | \code{NULL}\cr
Basis dimension passed to \code{mgcv::s()}. Defaults to \code{NULL}, which uses
\code{min(6, n_batches - 1)}. Floored at \code{3} and clamped to at most
\code{n_batches - 1} at training time.
\item \code{bs} :: \code{character(1)}\cr
Spline basis: \code{"cr"} (cubic regression spline, default; extrapolates
linearly beyond the boundary knots) or \code{"tp"} (thin plate).
}
}

