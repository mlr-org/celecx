% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerLCEParametricLogistic.R
\name{mlr_learners_lce.parametric_logistic}
\alias{mlr_learners_lce.parametric_logistic}
\alias{LearnerLCEParametricLogistic}
\title{Parametric Logistic LCE Learner}
\description{
Fits a four-parameter logistic learning curve
\deqn{f(b) = \ell + \frac{u - \ell}{1 + \exp(-k\,(b - b_0))}}
to the per-batch surrogate performance. Captures S-shaped trajectories with
a lower asymptote \eqn{\ell}, an upper asymptote \eqn{u}, a transition
midpoint \eqn{b_0}, and a steepness \eqn{k > 0}. Decreasing trajectories
are represented by \code{upper < lower} rather than by a negative rate.

The curve is fit on the task's \link{lce_link} scale. When \code{predict_type = "se"}
the learner reports the epistemic Gauss-Newton delta-method standard error
\code{se_epistemic} and the total predictive standard error \code{se} (adding the
residual variance), both on the link scale; predictive quantiles are the
exact Normal quantiles of that predictive.

Creates a new instance of this learner.
}
\section{Parameters}{

\itemize{
\item \code{lower_init}, \code{upper_init}, \code{midpoint_init}, \code{rate_init} :: \code{numeric(1)}\cr
Initial values for \code{lower}, \code{upper}, \code{midpoint}, and \code{rate}. Defaults are
derived from the training data when unset.
\item \code{rate_lower} :: \code{numeric(1)}\cr
Lower bound for \code{rate}. Initialized to \code{1e-6}.
\item \code{maxit} :: \code{integer(1)}\cr
Maximum optim iterations. Initialized to \code{500}.
}
}

