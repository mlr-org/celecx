% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ALDistance.R
\name{ALDistance}
\alias{ALDistance}
\title{Active Learning Distance Base Class}
\arguments{
\item{id}{(\code{character(1)})\cr
Identifier of the distance.}

\item{packages}{(\code{character()})\cr
Optional package names required by the distance.}

\item{param_set}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Configuration parameter set.}

\item{label}{(\code{character(1)})\cr
Label for the object.}

\item{man}{(\code{character(1)})\cr
String in the format \verb{[pkg]::[topic]} pointing to a manual page.}

\item{search_space}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Search space describing the candidate-pool columns.}

\item{xdt}{(\code{NULL} | \code{data.table})\cr
Configurations in search-space coordinates. For \code{fit_pool()} only,
\code{NULL} fits the distance to the search space itself where supported.}

\item{i}{(\code{NULL} | \code{integerish()})\cr
Optional subset of reference-point indices. \code{NULL} uses all reference
points.}
}
\value{
\code{self}.

\code{self}.

Numeric matrix with one row per query point and one column per
selected reference point.

\code{self}.
}
\description{
Abstract base class for active-learning distances.

An \code{ALDistance} is fitted on a candidate pool or, when the \code{xdt} passed to
\verb{$fit_pool()} is \code{NULL}, on the search space itself. Concrete distance
classes support search-space fitting where the search-space bounds define the
scaling or encoding; classes that need empirical pool information throw an
error for \code{NULL} pools. Reference points are set separately before distances
are computed.

Creates a new active-learning distance.

Fit the distance on a finite candidate pool or on the search space.

Set the reference points used by \code{distances()}.

Compute distances from query points to reference points indexed by \code{i}.

Clear all fitted state.
}
\section{Fields}{

\describe{
\item{\code{label}}{(\code{character(1)})
Label for this object.}

\item{\code{man}}{(\code{character(1)})
String in the format \verb{[pkg]::[topic]} pointing to a manual page.}

\item{\code{packages}}{(\code{character()})
Required packages.}

\item{\code{is_fitted}}{(\code{logical(1)})
Whether the distance was fitted on a pool or search space.}

\item{\code{search_space}}{(\link[paradox:ParamSet]{paradox::ParamSet} | \code{NULL})
Search space used during fitting.}

\item{\code{n_pool}}{(\code{integer(1)} | \code{NULL})
Number of pool points. \code{NULL} if the distance is not fitted or was
fitted to the search space without a finite pool.}

\item{\code{n_reference_points}}{(\code{integer(1)} | \code{NULL})
Number of reference points set for distance computation.}

\item{\code{state}}{(\code{any})
Fitted state returned by the subclass.}
}}

\section{Dictionary}{

This class can be retrieved from \link{mlr_al_distances} via \code{\link[=clx_ald]{clx_ald()}} and
\code{\link[=clx_alds]{clx_alds()}}.
}

\seealso{
Other ALDistance: 
\code{\link{ALDistanceGeometry}},
\code{\link{mlr_al_distances}},
\code{\link{mlr_al_distances_affine}},
\code{\link{mlr_al_distances_gower}},
\code{\link{mlr_al_distances_standardize}}
}
\concept{ALDistance}
