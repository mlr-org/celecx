% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer_pool_al.R
\name{optimizer_pool_al}
\alias{optimizer_pool_al}
\title{Convenience Constructor for Pool-Based Active Learning Optimizers}
\usage{
optimizer_pool_al(
  method = c("gsx", "gsy", "igs", "qbc", "random", "ideal"),
  learner = NULL,
  delta = 1,
  n_init = NULL,
  init_method = NULL,
  k_qbc = 5L,
  batch_size = 1L,
  pool_size = NULL
)
}
\arguments{
\item{method}{(\code{character(1)})\cr
One of \code{"gsx"}, \code{"gsy"}, \code{"igs"}, \code{"qbc"}, \code{"random"}, \code{"ideal"}.}

\item{learner}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr} | \code{NULL})\cr
Regression learner. Required for \code{"gsy"}, \code{"igs"}, \code{"qbc"}, \code{"ideal"}.}

\item{delta}{(\code{numeric(1)})\cr
Exploration weight for IDEAL (default 1).}

\item{n_init}{(\code{integer(1)} | \code{NULL})\cr
Number of initial samples. \code{NULL} uses \link{OptimizerAL}'s default
initialization policy.}

\item{init_method}{(\code{character(1)} | \code{NULL})\cr
Optional initialization override: \code{"gsx"}, \code{"random"}, or \code{"kmeans"}.
\code{NULL} keeps the method-specific default (\code{"kmeans"} for IDEAL,
\code{"gsx"} for GSx/GSy/iGS, \code{"random"} for random/QBC).}

\item{k_qbc}{(\code{integer(1)})\cr
Number of QBC committee members (default 5).}

\item{batch_size}{(\code{integer(1)})\cr
Points per iteration (default 1).}

\item{pool_size}{(\code{NULL} | \code{integer(1)})\cr
Optional number of candidate points to subsample uniformly before scoring.
\code{NULL} keeps exhaustive pool scoring and does not enable continuous-space
use.}
}
\value{
A configured \link{OptimizerAL}.
}
\description{
Creates an \link{OptimizerAL} with appropriate components for a specific active
learning method.
}
