% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_objective.R
\name{objective_learner_from_archive}
\alias{objective_learner_from_archive}
\title{Build an \code{ObjectiveLearner} from Archive Data}
\usage{
objective_learner_from_archive(
  oracle,
  search_space,
  codomain,
  x_data,
  pool = NULL
)
}
\arguments{
\item{oracle}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr})\cr
Trained regression learner mapping the search-space columns to the
objective value.}

\item{search_space}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Search space whose ids equal the oracle's training features.}

\item{codomain}{(\link[bbotk:Codomain]{bbotk::Codomain})\cr
Codomain of the originating run.}

\item{x_data}{(\link[data.table:data.table]{data.table::data.table})\cr
Archive feature values used to impute finite bounds for unbounded
parameters.}

\item{pool}{(\link[data.table:data.table]{data.table::data.table} | \code{NULL})\cr
Optional candidate pool whose values are also covered by the imputed
bounds.}
}
\value{
Named list with \code{objective} (\link{ObjectiveLearner}) and \code{search_space}
(the normalized \link[paradox:ParamSet]{paradox::ParamSet}).
}
\description{
Wraps a trained regression learner (the "oracle") as an \link{ObjectiveLearner}
over a normalized version of a search space, suitable for replaying an
active-learning loop forward.
}
\details{
Normalization makes the search space usable as an untransformed proposal box:
any transformation is dropped (the oracle is fit on, and proposals are made
on, the untransformed scale), and non-finite numeric bounds are imputed from
the range of the supplied data so samplers and terminators have a finite box.
Only \code{ParamDbl}, \code{ParamInt}, \code{ParamFct}, and \code{ParamLgl} parameters are
supported.
}
\keyword{internal}
