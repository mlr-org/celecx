% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lce_link.R
\name{lce_link}
\alias{lce_link}
\alias{lce_link_from_range}
\title{Predictive Link Functions for LCE Forecasts}
\usage{
lce_link(name)

lce_link_from_range(range)
}
\arguments{
\item{name}{(\code{character(1)})\cr
Name of a registered link.}

\item{range}{(\code{numeric(2)})\cr
Theoretical lower and upper bound of the metric.}
}
\value{
A link (named \code{list}).

\code{lce_link_from_range()} returns a \code{character(1)} link name.
}
\description{
A \link{LearnerLCE} models the surrogate-performance curve \code{f(b)} as Gaussian on a
\emph{link scale}: \code{g(f(b)) ~ Normal(mu, sigma^2)}. The link \code{g} maps the support
of the target metric to the whole real line, so that a location-scale normal
on the link scale induces a sensibly-shaped, support-respecting predictive
distribution on the natural scale (e.g. log-normal for non-negative losses,
logit-normal for \verb{[0, 1]} scores).

This is \emph{not} a general distribution object: the predictive is always
"normal on a link scale", carried by the plain numeric \code{response} / \code{se}
columns of a \link{PredictionLCE}. Only the link \code{g} varies. All links are monotone
\emph{increasing} maps from their support to the reals, so quantiles and tail
probabilities map through \code{g} / \code{g^{-1}} without sign bookkeeping.

A link is a plain list with elements \code{name}, \code{transform} (\code{g}), \code{inverse}
(\code{g^{-1}}), and \code{support} (the natural-scale interval \code{g} maps to the reals).
Retrieve one by name with \code{lce_link()}. The built-in links are:
\itemize{
\item \code{"identity"}: support \verb{(-Inf, Inf)}. For unbounded metrics.
\item \code{"log"}: support \verb{(0, Inf)}. For non-negative losses (MAE, RMSE, MSE);
induces a log-normal predictive.
\item \code{"logit"}: support \verb{(0, 1)}. For bounded scores (accuracy, AUC); induces a
logit-normal predictive.
}

\code{lce_link_from_range()} picks a sensible link name from a metric's theoretical
\code{range}: \code{"log"} for \verb{(0, Inf)}, \code{"logit"} for \verb{(0, 1)}, and \code{"identity"}
otherwise. This expresses the idea that the link belongs to the target metric,
but the choice is never applied silently: a \link{TaskLCE} uses whatever \code{link} it
was constructed with (\code{"identity"} by default).
}
