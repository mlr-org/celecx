% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective_helpers.R
\name{assert_domain_codomain}
\alias{assert_domain_codomain}
\title{Validate Domain and Codomain Structure}
\usage{
assert_domain_codomain(domain, codomain)
}
\arguments{
\item{domain}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Parameter set describing the input space.}

\item{codomain}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Parameter set describing the output space.}
}
\value{
Named list with \code{domain_ids}, \code{codomain_ids}, and \code{codomain_target_ids}.
}
\description{
Validates that a domain and codomain have valid structure for use with
Objective classes. Uses paradox's \code{assert_param_set} for type validation
and adds checks for non-empty domain and presence of targets.
}
\details{
Uses \code{assert_param_set(domain, no_untyped = TRUE)} which rejects ParamUty.
Note that paradox's \verb{$check_dt()} cannot be used for construction-time validation
because we accept data formats that paradox doesn't (e.g., factor columns for
ParamFct, which paradox expects as character). Our validation handles the
conversion/coercion that prepares data for paradox-compatible queries.

As of bbotk >= 0.9.0, codomains can have "learn" tags in addition to
"minimize"/"maximize", so we check for any target tags.
}
\keyword{internal}
