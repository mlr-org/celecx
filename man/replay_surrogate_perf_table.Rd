% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replay_surrogate_performance.R
\name{replay_surrogate_perf_table}
\alias{replay_surrogate_perf_table}
\title{Offline Surrogate-Performance Table}
\usage{
replay_surrogate_perf_table(
  archive,
  learner,
  task,
  measures = list(msr("regr.rsq"), msr("regr.mae"))
)
}
\arguments{
\item{archive}{(\link[bbotk:ArchiveBatch]{bbotk::ArchiveBatch})\cr
Archive of a finished single-target run.}

\item{learner}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr} | \link[mlr3mbo:SurrogateLearner]{mlr3mbo::SurrogateLearner})\cr
Surrogate model refit per batch; see \code{\link[=replay_surrogate_performance]{replay_surrogate_performance()}}.}

\item{task}{(\link[mlr3:TaskRegr]{mlr3::TaskRegr})\cr
Held-out regression task to score on.}

\item{measures}{(\code{list()} of \link[mlr3:Measure]{mlr3::Measure})\cr
Regression measures; named entries use their names as column names.}
}
\value{
\link[data.table:data.table]{data.table::data.table} with columns \code{batch_nr}, \code{n_evals}, and one numeric column
per measure.
}
\description{
The scoring core of \code{\link[=replay_surrogate_performance]{replay_surrogate_performance()}}: refits \code{learner} on every archive batch
prefix, scores it on the held-out \code{task}, and returns the per-batch performance table with
one row per batch and one column per measure -- the same shape
\link{CallbackSurrogatePerformance} records online (columns \code{batch_nr}, \code{n_evals}, then the
measure columns). Use this when the full multi-measure table is wanted rather than a
single-target \link{TaskLCE}.
}
