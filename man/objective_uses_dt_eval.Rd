% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_objective.R
\name{objective_uses_dt_eval}
\alias{objective_uses_dt_eval}
\title{Detect Objectives with Native data.table Evaluation Semantics}
\usage{
objective_uses_dt_eval(objective)
}
\arguments{
\item{objective}{(\link[bbotk:Objective]{bbotk::Objective})\cr
Objective to inspect.}
}
\value{
\code{logical(1)}.
}
\description{
Returns whether an objective explicitly advertises that its \code{eval_dt()}
implementation preserves the missing-value semantics expected by
\link{SearchInstance}.
}
\details{
This is an explicit S3 capability instead of a method-body heuristic.
Merely overriding \code{eval_dt()} is not sufficient: subclasses can wrap or
forward to \link[bbotk:Objective]{bbotk::Objective}'s default \code{eval_dt()}, which still converts
rows with \code{transpose_list()} and therefore treats dependency-inactive
parameters as literal \code{NA} values instead of dropping them. Native
data-table objectives opt in with an S3 method, and subclasses inherit that
capability automatically.
}
\keyword{internal}
