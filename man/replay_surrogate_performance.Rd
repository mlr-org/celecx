% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replay_surrogate_performance.R
\name{replay_surrogate_performance}
\alias{replay_surrogate_performance}
\title{Offline Surrogate-Performance Replay}
\usage{
replay_surrogate_performance(
  archive,
  learner,
  task,
  measures = list(msr("regr.rsq"), msr("regr.mae")),
  measure = NULL,
  pool = NULL,
  link = "identity",
  id = "surrogate_performance",
  label = NA_character_
)
}
\arguments{
\item{archive}{(\link[bbotk:ArchiveBatch]{bbotk::ArchiveBatch})\cr
Archive of a finished single-target run.}

\item{learner}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr} | \link[mlr3mbo:SurrogateLearner]{mlr3mbo::SurrogateLearner})\cr
Surrogate model refit per batch. A plain regression learner is wrapped in a
\link[mlr3mbo:SurrogateLearner]{mlr3mbo::SurrogateLearner} so the fit / predict / output-transform path
matches a live run; a surrogate is used as given (cloned). Either way the
object is cloned, so the caller's model is left untrained.}

\item{task}{(\link[mlr3:TaskRegr]{mlr3::TaskRegr})\cr
Held-out regression task to score on. Must contain the archive's
search-space columns as features.}

\item{measures}{(\code{list()} of \link[mlr3:Measure]{mlr3::Measure})\cr
Regression measures. Named entries use their names as performance-column
names; unnamed entries use the measure id.}

\item{measure}{(\code{character(1)} | \code{NULL})\cr
Name of the measure to use as the \link{TaskLCE} target. When \code{NULL} (default)
and a single measure is given, that measure is used.}

\item{pool}{(\link[data.table:data.table]{data.table::data.table} | \code{NULL})\cr
Finite candidate pool of the originating run, carried into the resulting
\link{TaskLCE} for pool-based replay. Its columns must be the archive's
search-space ids. \code{NULL} (default) for continuous runs.}

\item{link}{(\code{character(1)})\cr
Name of the predictive \link{lce_link} for the resulting \link{TaskLCE}. \code{"identity"}
by default; pass e.g. \code{"log"} for a non-negative loss metric (see
\link{lce_link_from_range}).}

\item{id}{(\code{character(1)})\cr
Task id.}

\item{label}{(\code{character(1)})\cr
Optional task label.}
}
\value{
\link{TaskLCE}.
}
\description{
Reconstructs the surrogate-performance learning curve of a finished
active-learning run, offline, from its \link[bbotk:ArchiveBatch]{bbotk::ArchiveBatch}. For every batch
the surrogate is refit on the archive data available up to that batch and
scored on a held-out regression task, yielding one performance value per
batch and measure. The result is assembled into a \link{TaskLCE}, the same shape
produced online by \link{CallbackSurrogatePerformance}.

This is the offline twin of \link{CallbackSurrogatePerformance}: the callback
scores the surrogate the optimizer maintains during the run, whereas this
function replays the run from a stored archive, refitting \code{learner} itself.
}
