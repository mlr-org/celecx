% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OptimizerGS.R
\name{mlr_optimizers_gs}
\alias{mlr_optimizers_gs}
\alias{OptimizerGS}
\title{Greedy Sampling Optimizer}
\arguments{
\item{scoring}{(\code{function})\cr
Scoring function closure from \link{scoring_functions}.}

\item{learner}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr} | \code{NULL})\cr
Optional regression learner for model-based methods (GSy, iGS, QBC).}
}
\description{
Pool-based active learning optimizer implementing the greedy sampling family
(GSx, GSy, iGS, QBC, random) specified in Wu et al., "Active Learning for
Regression using Greedy Sampling". Uses pluggable scoring closures created by
the factory functions in \link{scoring_functions}.

Features are standardized to mean 0, sd 1 for distance computations.
The regression learner is trained and queried on raw (unscaled) features,
keeping the learner's input contract independent of the distance scaling.
By default, GSx / GSy / iGS initialize with \code{\link[=gsx_init]{gsx_init()}} while random / QBC
initialize by random sampling, matching the paper-family defaults. This can
be overridden via \code{init_method}.

The optimizer targets \link{SearchInstance} with an \link{ObjectiveDataset} objective.
It is not routed through the MBO acquisition function pipeline.

Creates a new instance of this \link[R6:R6Class]{R6} class.
}
\section{Fields}{

\describe{
\item{\code{scoring}}{(\code{function})\cr
Scoring closure. See \link{scoring_functions}.}

\item{\code{learner}}{(\link[mlr3:LearnerRegr]{mlr3::LearnerRegr} | \code{NULL})\cr
Regression learner for model-based methods. \code{NULL} for GSx/random.}
}}

\section{Parameters}{

\itemize{
\item \code{n_init} (\code{integer(1)})\cr
Number of initial samples via GSx before model-based scoring begins.
Default: dimension of search space (post-preprocessing).
\item \code{batch_size} (\code{integer(1)})\cr
Points per iteration. Exact batching is implemented for \code{"random"} and
\code{"gsx"}. Other greedy-sampling methods currently require \code{batch_size = 1}.
\item \code{init_method} (\code{character(1)})\cr
Initialization rule for the first \code{n_init} samples: \code{"gsx"} (centroid-
nearest + farthest-first), \code{"random"}, or \code{"kmeans"} (K-means clustering).
Defaults to the paper-style method family default.
}
}

\section{Construction}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{OptimizerGS$new(scoring, learner = NULL)
}\if{html}{\out{</div>}}

Or use the convenience constructor \code{\link[=optimizer_pool_al]{optimizer_pool_al()}}.
}

