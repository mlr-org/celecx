% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PredictionLCE.R
\name{PredictionLCE}
\alias{PredictionLCE}
\alias{check_prediction_data.PredictionDataLCE}
\alias{is_missing_prediction_data.PredictionDataLCE}
\alias{filter_prediction_data.PredictionDataLCE}
\alias{c.PredictionDataLCE}
\title{LCE Prediction Object}
\usage{
\method{check_prediction_data}{PredictionDataLCE}(pdata, ...)

\method{is_missing_prediction_data}{PredictionDataLCE}(pdata, ...)

\method{filter_prediction_data}{PredictionDataLCE}(pdata, row_ids, ...)

\method{c}{PredictionDataLCE}(..., keep_duplicates = TRUE)
}
\arguments{
\item{row_ids}{(\code{integer()})\cr
Row ids of the predictions.}

\item{task}{(\link{TaskLCE})\cr
Task used to derive row ids and truth.}

\item{truth}{(\code{numeric()})\cr
True surrogate performances.}

\item{response}{(\code{numeric()})\cr
Predicted surrogate performances (natural-scale predictive median).}

\item{se}{(\code{numeric()})\cr
Total predictive standard error on the link scale.}

\item{se_epistemic}{(\code{numeric()})\cr
Epistemic standard error of the mean on the link scale.}

\item{quantiles}{(\code{matrix()})\cr
Predicted quantiles (rows = observations, columns = probabilities). The
probabilities must be stored in the \code{"probs"} attribute.}

\item{samples}{(\code{matrix()})\cr
Predictive draws (rows = observations, columns = draws).}

\item{target_reached}{(\code{matrix()})\cr
Reach probabilities (rows = observations, columns = targets). The targets
must be stored in the \code{"target"} attribute.}

\item{weights}{(\code{numeric()})\cr
Optional measure weights.}

\item{check}{(\code{logical(1)})\cr
Whether to validate the inputs.}
}
\description{
Prediction object for \link{TaskLCE}. Carries the per-row true and predicted
surrogate performance and, depending on the producing learner's predict type,
a distributional payload describing the predictive of the performance curve
\code{f(b)}:
\itemize{
\item \code{response} (\code{numeric()}): predictive median on the natural scale. For the
sample-based learners this is the mean of the draws on the \link{lce_link} scale,
back-transformed -- which is the median whenever the link-scale distribution
is symmetric (e.g. Gaussian). Always present.
\item \code{se} (\code{numeric()}): \emph{total} predictive standard deviation on the link scale
(epistemic + aleatoric), the quantity to score against the realised \code{y_b}.
\item \code{se_epistemic} (\code{numeric()}): standard deviation of the \emph{mean} \code{f(b)} on the
link scale (epistemic only), the quantity for expected-crossing decisions.
\item \code{quantiles} (\code{matrix()}): predictive quantiles, one row per observation, one
column per probability (carried in the \code{"probs"} attribute).
\item \code{samples} (\code{matrix()}): predictive draws, one row per observation, one column
per draw. Columns are joint sample paths for the sample-based learners.
\item \code{target_reached} (\code{matrix()}): probability that the metric has reached a
target, one row per observation, one column per target (carried in the
\code{"target"} attribute).
}

The link scale on which \code{se} / \code{se_epistemic} live is a property of the
\link{TaskLCE} (its \code{link}); the prediction itself stays a few plain numeric /
matrix columns.

Creates a new PredictionLCE.
}
\section{Fields}{

\describe{
\item{\code{response}}{(\code{numeric()})\cr
Predicted surrogate performance for each row.}

\item{\code{se}}{(\code{numeric()})\cr
Total predictive standard error (link scale), or \code{NA} vector when absent.}

\item{\code{se_epistemic}}{(\code{numeric()})\cr
Epistemic standard error of the mean (link scale), or \code{NA} when absent.}

\item{\code{quantiles}}{(\code{matrix()})\cr
Matrix of predicted quantiles (rows = observations, columns ascending).}

\item{\code{samples}}{(\code{matrix()})\cr
Matrix of predictive draws (rows = observations, columns = draws).}

\item{\code{target_reached}}{(\code{matrix()})\cr
Matrix of reach probabilities (rows = observations, columns = targets).}
}}

