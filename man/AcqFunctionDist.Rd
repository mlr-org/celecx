% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AcqFunctionDist.R
\name{AcqFunctionDist}
\alias{AcqFunctionDist}
\title{Distance-Aware Acquisition Function Base Class}
\arguments{
\item{id}{(\code{character(1)})\cr
Acquisition function id.}

\item{constants}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Changeable constants or parameters of the acquisition function.}

\item{surrogate}{(\code{NULL} | \link[mlr3mbo:Surrogate]{mlr3mbo::Surrogate})\cr
Surrogate whose predictions are used in the acquisition function.}

\item{al_distance}{(\code{NULL} | \link{ALDistance})\cr
Distance object used by the acquisition function.}

\item{requires_predict_type_se}{(\code{logical(1)})\cr
Whether the surrogate must use predict type \code{"se"}.}

\item{direction}{(\code{"same"} | \code{"minimize"} | \code{"maximize"})\cr
Optimization direction of the acquisition function.}

\item{packages}{(\code{character()})\cr
Required packages.}

\item{label}{(\code{character(1)})\cr
Label for the object.}

\item{man}{(\code{character(1)})\cr
String in the format \verb{[pkg]::[topic]} pointing to a manual page.}

\item{xdt}{(\code{NULL} | \code{data.table})\cr
Candidate configurations in search-space coordinates. \code{NULL} fits to
the search space where supported.}

\item{search_space}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Search space describing the candidate-pool columns.}
}
\value{
\code{self}.
}
\description{
Abstract base class for distance-aware active-learning acquisition functions.

The acquisition constants include a detached copy of the wrapped
\link{ALDistance} parameter set under the \code{al_distance} prefix. During
update and evaluation, those values are copied into the distance before
reference points are set or distances are computed.

\verb{$fit_pool()} fits the distance on the candidate pool used for scaling and
pool-derived acquisition state, or on the search space when \code{xdt} is \code{NULL}.
\verb{$update()} then sets the distance reference points to the current
archive rows in archive order.

Creates a new distance-aware acquisition function.

Fits the wrapped distance and any subclass pool state on a finite
candidate pool or on the search space.

Sets the distance reference points to the current surrogate archive.
}
\section{Fields}{

\describe{
\item{\code{al_distance}}{(\code{NULL} | \link{ALDistance})
Distance object used by the acquisition function.}
}}

\seealso{
Other Acquisition Function: 
\code{\link{AcqFunctionDistGSx}},
\code{\link{AcqFunctionDistIDEAL}},
\code{\link{AcqFunctionDistIGS}},
\code{\link{AcqFunctionGSy}},
\code{\link{SurrogateNull}}
}
\concept{Acquisition Function}
