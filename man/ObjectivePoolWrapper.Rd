% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ObjectivePoolWrapper.R
\name{ObjectivePoolWrapper}
\alias{ObjectivePoolWrapper}
\title{Objective Function Wrapping Another Objective on a Candidate Pool}
\arguments{
\item{pool}{(\code{data.frame} | \code{data.table})\cr
Candidate pool containing one row per admissible configuration. Must
contain all wrapped-objective domain columns.}

\item{objective}{(\link[bbotk:Objective]{bbotk::Objective})\cr
Objective to wrap. It is cloned on construction.}

\item{id}{(\code{character(1)})\cr
Identifier for the objective.}

\item{domain}{(\link[paradox:ParamSet]{paradox::ParamSet} | \code{NULL})\cr
Parameter set describing the input space. If \code{NULL}, the wrapped objective's domain is used.}

\item{check_values}{(\code{logical(1)})\cr
Whether to check validity of input configurations against the domain.
This does not affect the wrapped objective's \code{check_values}.
When \code{domain} is \code{NULL}, the wrapped objective's \code{check_values} is set to \code{FALSE}.
Otherwise, it is unchanged; it is up to the user to decide whether it is desirable for the
wrapped objective to check values.}
}
\description{
An \link{ObjectivePoolAbstract} that restricts evaluation to a candidate pool and
delegates evaluation to another \link[bbotk:Objective]{bbotk::Objective}.

Creates a new instance of this \link[R6:R6Class]{R6} class.
}
\details{
The wrapped objective is cloned on construction. Its codomain,
properties, and package requirements define the corresponding fields of the
wrapper.

The wrapped objective's domain may differ from the wrapper's domain;
in this case, the missing values are reconstructed from the pool columns and
superfluous columns are dropped.
The pool values must be unique with respect to the wrapper's domain.
}
\section{Fields}{

\describe{
\item{\code{objective}}{(\link[bbotk:Objective]{bbotk::Objective})\cr
Wrapped objective.}
}}

