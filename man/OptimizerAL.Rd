% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OptimizerAL.R
\name{OptimizerAL}
\alias{OptimizerAL}
\title{Proposer-Based Active Learning Optimizer}
\arguments{
\item{proposer}{(\link{ALProposer})\cr
Proposer used after initialization.}

\item{surrogates}{(named \code{list()} of \link[mlr3mbo:Surrogate]{mlr3mbo::Surrogate})\cr
Canonical surrogate registry. Defaults to an archive-only surrogate
under id \code{"archive"} for model-free acquisition functions.}

\item{acq_functions}{(named \code{list()} of \link[mlr3mbo:AcqFunction]{mlr3mbo::AcqFunction})\cr
Acquisition-function prototype registry. Registered acquisition
functions are treated as unwired prototypes; any pre-set surrogate is
removed with a warning.}

\item{init_sampler}{(\code{NULL} | \link{SpaceSampler})\cr
Sampler for the initial evaluations. \code{NULL} is only valid when
\code{n_init = 0}.}

\item{result_assigner}{(\code{NULL} | \link[mlr3mbo:ResultAssigner]{mlr3mbo::ResultAssigner})\cr
Result assigner used for \link[bbotk:OptimInstance]{bbotk::OptimInstance} objects. Defaults to
\link{ResultAssignerNull}.}

\item{grid_expansion_limit}{(\code{integer(1)})\cr
Upper limit for fully-discrete grid expansion inherited from
\link{OptimizerPoolAbstract}.}
}
\description{
Active-learning optimizer whose outer loop is fixed and whose proposal logic
is delegated to an \link{ALProposer}.

The optimizer owns canonical run-local surrogates and unwired acquisition
function prototypes. Proposers refer to those objects by registry id; the
ephemeral \link{ALContext} wires and updates them lazily.

Creates a new active-learning optimizer.
}
\section{Fields}{

\describe{
\item{\code{proposer}}{(\link{ALProposer})
Proposer used after initialization.}

\item{\code{init_sampler}}{(\code{NULL} | \link{SpaceSampler})
Sampler for initial evaluations.}

\item{\code{surrogates}}{(named \code{list()} of \link[mlr3mbo:Surrogate]{mlr3mbo::Surrogate})
Canonical surrogate registry.}

\item{\code{acq_functions}}{(named \code{list()} of \link[mlr3mbo:AcqFunction]{mlr3mbo::AcqFunction})
Unwired acquisition-function prototype registry.}

\item{\code{result_assigner}}{(\code{NULL} | \link[mlr3mbo:ResultAssigner]{mlr3mbo::ResultAssigner})
Result assigner.}

\item{\code{param_set}}{(\link[paradox:ParamSet]{paradox::ParamSet})
Combined parameter set of the optimizer and directly owned components.}
}}

\section{Parameters}{

\describe{
\item{\code{batch_size}}{\code{integer(1)}\cr
Number of configurations evaluated per active-learning proposal round.}
\item{\code{replace_samples}}{\code{character(1)}\cr
Whether finite-pool points can be proposed again after a batch.
\code{"never"} exhausts the pool without replacement; \code{"between_batches"}
allows repeat evaluations in later batches, while still preventing repeats
within the current proposal batch.}
\item{\code{n_init}}{\code{integer(1)}\cr
Number of initial evaluations requested before the proposer is used.
If unset, fresh runs use \code{4 * d} initial evaluations, where \code{d} is the
search-space dimension, while runs with an already populated archive do not
request additional initial evaluations.}
}

The optimizer parameter set also exposes the parameter sets of directly owned
components: \code{init_sampler}, \code{proposer}, \code{surrogates}, and acquisition-function
\code{constants}.
}

